$(function() {
    $("#select-all-workers").change(function() {
        $(".select-workers").prop("checked", this.checked);
    });
    $("#select-all-pending").change(function() {
        $(".select-pending").prop("checked", this.checked);
    });
});
